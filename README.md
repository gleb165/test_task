# Test Task — Social-Media Comments App

## Описание  
Этот проект — простое web-приложение для публикации, отображения и лайков комментариев с возможностью аутентификации пользователей.  
Backend реализован на Django + Django REST Framework, фронтенд — на React + Vite. Всё завернуто в контейнеры через Docker + docker-compose, для простого запуска — “всё в одном”.  

Проект демонстрирует, как собрать full-stack приложение с API, SPA интерфейсом, real-time обновлением по WebSocket, аутентификацией и хранением комментариев.  

---

## Фичи  

- CRUD-функционал для комментариев (создание, отображение, лайки, вложения, ответы)  
- REST API на Django + DRF  
- SPA интерфейс на React + Vite — таблица комментариев, сортировка, модальные формы  
- Реальное время — WebSocket (для новых комментариев)  
- Поддержка фронтенда + бэкенда + БД (PostgreSQL), Redis и других сервисов через Docker Compose  
- Стек контейнеризирован — легко запускать, тестировать, переносить  

---

## Технологии  

**Backend**  
- Python 3.x  
- Django  
- Django REST Framework  
- PostgreSQL  
- Redis
- Celery / RabbitMQ  

**Frontend**  
- React + Vite  
- HTML / CSS / JavaScript  

**DevOps / контейнеры**  
- Docker & docker-compose  
- Nginx (внутри контейнера фронтенда, для отдачи билдов и проксирования API)  

---

## 📝 Предварительные условия  

- Установлены Docker и docker-compose  
- На машине должен быть доступен Docker (и, в зависимости от настроек, достаточный объём RAM/CPU)  

---
## ⚙️ Environment variables (.env)

Перед запуском проекта нужно создать файл .env в корне проекта и заполнить его параметрами.
Файл НЕ коммитим в GitHub (он уже есть в .gitignore).

Пример содержимого .env:
# Django settings
SECRET_KEY=ваш_секретный_ключ
DEBUG=True

# AWS (если не используете — оставить пустым)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=

# Email settings
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_HOST_USER=youremail@gmail.com
EMAIL_HOST_PASSWORD=пароль_приложения_почты
EMAIL_USE_TLS=True
DEFAULT_FROM_EMAIL=YourApp <youremail@gmail.com>

# Database (PostgreSQL)
DB_ENGINE=django.db.backends.postgresql
DB_NAME=test-task-db
DB_USER=postgres_username
DB_PASSWORD=postgres_password
DB_HOST=postgres
DB_PORT=5432

# RabbitMQ
RABBITMQ_USER=rabbitmq_user
RABBITMQ_PASSWORD=rabbitmq_pass
RABBITMQ_HOST=rabbitmq
RABBITMQ_PORT=5672
RABBITMQ_URL=amqp://rabbitmq_user:rabbitmq_pass@rabbitmq:5672//

# Redis
REDIS_URL=redis://redis:6379/1
REDIS_HOST=redis


## ▶️ Как запустить  

1. Клонируйте репозиторий  

```bash
git clone https://github.com/gleb165/test_task.git
cd test_task 

2. Запустите всё через docker-compose

docker-compose up --build

3. Откройте в браузере http://localhost:5173


4. Если всё настроено верно — фронтенд загрузится, интерфейс для комментариев будет доступен, и API за бэкендом будет работать «под капотом». ```


5. Перезапуск и остановка 
docker-compose down        # остановка всех контейнеров  
docker-compose up --build  # пересборка и запуск заново  


